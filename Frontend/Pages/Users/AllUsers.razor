@page "/users"

@if (string.IsNullOrEmpty(errorMessage))
{
    <h3 class="text-center mb-2">المستخدمين</h3>
    <hr class="mb-3" />

    <a class="btn btn-primary border-2 mb-5" href="/users/add">أضافة مستخدم</a>
    @if (Users.Count > 0)
    {
        <table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>الإسم</th>
            <th>البريد الإلكتروني</th>
            <th>الصلاحية</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Users)
                {
                    <tr>
                        <td>@user.FullName</td>
                        <td>@user.Email</td>
                        <td>@Enum.GetName(user.Role)</td>
                        <td>
                            <a href="/users/edit/" class="btn btn-primary mx-3">
                                <i class="lni lni-pen-to-square"></i>
                            </a>
                            <a href="/users/delete/" class="btn btn-danger">
                                <i class="lni lni-trash-3"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info text-center">لايوجد مستخدمين حالياً</div>
    }
}
else
{
    <div class="alert alert-danger text-center">@errorMessage</div>
}
